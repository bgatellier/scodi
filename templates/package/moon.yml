$schema: ../../.moon/cache/schemas/project.json

language: typescript
project:
  description: Analyses URLs with {{ name }}
  title: Scodi {{ name }}
stack: backend
tags:
- scodi
- analysis
layer: application
